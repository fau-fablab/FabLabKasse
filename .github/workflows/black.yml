name: Black Code Style

on: [push, pull_request]

jobs:
  black-code-style:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: psf/black@stable
        with:
          version: "21.9b0 click==7.0"  # dirty hack for broken dependency